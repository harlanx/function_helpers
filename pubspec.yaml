name: function_helpers
description: A starting point for Dart libraries or applications.
version: 1.0.1
# homepage: https://www.example.com

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^4.0.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.2
